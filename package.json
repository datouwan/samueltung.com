{
  "name": "samueltung-com",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy && node scripts/purge-cache.mjs",
    "purge": "node scripts/purge-cache.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
